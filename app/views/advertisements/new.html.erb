<h1>Advertisements#new</h1>
<p>Find me in app/views/advertisements/new.html.erb</p>
<%= link_to "Back", :back %>
	<p>
		<h3>
			Fill the form to add your hotel
		</h3>
	</p>

	<%= form_for @advertisement, :html => { :multipart => true } do |f| %>

	<p><%= f.label :image %></p>
	<%= f.file_field :image %>
   <p> <%= f.label :author %></p>
    <%= f.text_field :author %>
  <p>  <%= f.label :content %></p>
    <%= f.text_area :content %>

<br />
 	<%= f.submit "Create new advertisement"%>
	<% end %>