%div.container
	= form_for [@advertisement,@comment] do |f|
		= render 'shared/error_messages', object: f.object
		%p=f.label :content
		=f.text_area :content
		%br
		= f.submit 'Update comment',:class=>"btn btn-primary"
